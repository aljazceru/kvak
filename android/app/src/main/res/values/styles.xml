<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- ponytail: fill any uncovered window area (e.g. below the keyboard when
             the IME draws the nav bar) with the app bg instead of the default black,
             and keep the nav bar color in sync with the app theme. -->
        <item name="android:windowBackground">@color/app_bg_dark</item>
        <item name="android:navigationBarColor">@color/app_bg_dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>
